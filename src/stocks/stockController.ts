/*
const alpha = require("alphavantage")({ key: process.env.ALPHAVANTAGE_KEY })
alpha.data.intraday('msft').then((data: string) => {
    const polished = alpha.util.polish(data)
    console.log(polished)
})
*/